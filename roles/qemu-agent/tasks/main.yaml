---
- name: Install qemu-guest-agent
  become: true
  package:
    name: qemu-guest-agent
    state: present
    update_cache: yes

- name: Enable and start qemu-guest-agent
  become: true
  service:
    name: qemu-guest-agent
    state: started
    enabled: true
