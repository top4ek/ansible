---
- name: Ensure container project directory exists
  file:
    path: "{{ shizoid_container_dir }}"
    state: directory
    owner: "{{ admin_user }}"
    group: "{{ admin_user }}"
    mode: "0755"

- name: Run container
  include_role:
    name: podman
    tasks_from: container.yaml
  vars:
    container_name: "{{ shizoid_container_name }}"
    podman_compose_dest: "{{ shizoid_container_dir }}/compose.yaml"
    compose_project_dir: "{{ shizoid_container_dir }}"
