- name: Install Podman packages (common)
  become: true
  package:
    state: present
    update_cache: yes
    name:
      - podman
      - podman-compose
      - crun
      - slirp4netns
      - fuse-overlayfs

- name: Allow rootless bind to privileged ports (min 80)
  become: true
  ansible.builtin.sysctl:
    name: net.ipv4.ip_unprivileged_port_start
    sysctl_file: /etc/sysctl.d/rootless-ports.conf
    value: "80"
    state: present

- name: Run distribution specific tasks
  include_tasks: "{{ ansible_os_family }}.yaml"

- name: Ensure /etc/containers exists
  become: true
  file:
    path: /etc/containers
    state: directory
    owner: root
    group: root
    mode: "0755"

- name: Configure system containers.conf
  become: true
  template:
    src: templates/containers.conf.j2
    dest: /etc/containers/containers.conf
    owner: root
    group: root
    mode: "0644"

- name: Ensure user containers config directory exists
  become: true
  file:
    path: "/home/{{ admin_user }}/.config/containers"
    state: directory
    owner: "{{ admin_user }}"
    group: "{{ admin_user }}"
    mode: "0755"

- name: Configure user containers.conf (rootless)
  become: true
  template:
    src: templates/containers.conf.j2
    dest: "/home/{{ admin_user }}/.config/containers/containers.conf"
    owner: "{{ admin_user }}"
    group: "{{ admin_user }}"
    mode: "0644"

- name: Ensure subuid mapping for admin_user
  become: true
  lineinfile:
    path: /etc/subuid
    create: true
    mode: "0644"
    regexp: "^{{ admin_user }}:"
    line: "{{ admin_user }}:100000:65536"

- name: Ensure subgid mapping for admin_user
  become: true
  lineinfile:
    path: /etc/subgid
    create: true
    mode: "0644"
    regexp: "^{{ admin_user }}:"
    line: "{{ admin_user }}:100000:65536"

- name: Ensure container project directory exists
  file:
    path: "/home/{{ admin_user }}/containers"
    state: directory
    owner: "{{ admin_user }}"
    group: "{{ admin_user }}"
    mode: "0755"
